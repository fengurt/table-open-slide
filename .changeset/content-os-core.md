---
"@open-slide/core": minor
---

Add optional CMS-backed content layer: `ContentProvider` / `ContentRoot`, `<T>` / `useContentString`, extended `OpenSlideConfig` (`content`, `collaborationRole`), locale switcher wiring, Payload-backed slide comments when `content.apiBaseUrl` is set, and role-gated inspector controls.
